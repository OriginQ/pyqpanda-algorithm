# pyqpanda_alg

Quantum algorithm implementations for pyqpanda3, with local execution and
optional qpanda3-runtime support.

Install the base package with `pip install pyqpanda_alg`; install
`pyqpanda_alg[runtime]` when running algorithms through QPanda Runtime.
